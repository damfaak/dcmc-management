'use client';
import {useState} from 'react';
import {ShieldCheck,ArrowRight,LockKeyhole} from 'lucide-react';
import {InputOTP,InputOTPGroup,InputOTPSlot} from '@/components/ui/input-otp';
export default function Home(){const [pin,setPin]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');return <main className="login"><section className="login-brand"><div className="brand"><span className="mark">D</span>DCMC</div><div><div className="eyebrow">FACTION MANAGEMENT</div><h1>Keep the crew.<br/><span>In control.</span></h1><p>Satu tempat untuk mengelola player, inventory, dan setiap setoran DCMC.</p></div><div className="login-bottom eyebrow">LOS SANTOS · INTERNAL OPERATIONS</div></section><section className="login-form-wrap"><form className="login-form" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pin})});const d:any=await r.json();if(!r.ok)throw Error(d.error);location.href='/dashboard'}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}><ShieldCheck size={32} color="#ee454c"/><h2>Welcome to DCMC.</h2><p>Masukkan PIN akses untuk masuk ke<br/>Management System.</p><div className="pin-row"><InputOTP aria-label="Access PIN" maxLength={4} pattern="^[0-9]*$" value={pin} onChange={setPin}><InputOTPGroup>{[0,1,2,3].map(i=><InputOTPSlot key={i} index={i}/>)}</InputOTPGroup></InputOTP></div><button className="primary-btn w-full" disabled={busy||pin.length!==4}>{busy?'Memverifikasi…':'Login'}<ArrowRight size={17}/></button>{error&&<p className="error" role="alert">{error}</p>}<div className="login-footer"><LockKeyhole size={14}/>Akses hanya untuk anggota yang berwenang.</div></form></section></main>}
